# Docker interview questions

1. How do you reduce Docker image size for a Java/Python/Node app using multi-stage builds?
2. How do you debug a container that keeps failing inside Kubernetes?
3. How do you manage credentials/secrets inside containers securely?
4. What is the difference between ENTRYPOINT vs CMD in real deployment use cases?
5. Your container works locally but fails in Kubernetes — what is your debugging approach?
